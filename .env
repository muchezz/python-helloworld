# Phase 6B.1 real-acceptance fixture ONLY. Not a real secret, not used in
# production -- this repository has no database/queue/cache/session backing
# service, so every driver below is deliberately forced to a non-DB, no-
# external-service option (Phase 6B.1 explicitly excludes env/secrets support).
APP_NAME=NjiraCloudFixture
APP_ENV=production
APP_KEY=base64:LGTRCdorZs1MftFPxTMtH7nQz08RBzR8P9dTwyszrFg=
APP_DEBUG=false
APP_TIMEZONE=UTC
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=warning

DB_CONNECTION=sqlite

SESSION_DRIVER=array
CACHE_STORE=array
QUEUE_CONNECTION=sync
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
MAIL_MAILER=log
